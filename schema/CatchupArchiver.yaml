$schema: http://json-schema.org/draft-07/schema#
$id: https://github.com/lsst-dm/dm_CatchupArchiver/blob/master/schema/CatchupArchiver.yaml
# title must end with one or more spaces followed by the schema version, which must begin with "v"
title: CatchupArchiver v1
description: Schema for CatchupArchiver configuration files
type: object
forwarder:
    forwarder_consume_queue: f98_consume
    forwarder_publish_queue: cc_foreman_ack_publish
